@page
@model StudentsEducation.Web.Areas.Admin.Pages.Teachers_Schedules.DeleteModel

@{
    ViewData["Title"] = "Удаление преподавателя";
}

<h1>Преподаватель</h1>

<h3>Вы действительно хотите удалить преподавателя влкючая его расписание (оценки/пропуски/зачеты студентов)?</h3>
<div>
    <h4>Преподаватель</h4>
    <hr />
    <div class="row">
        <dl class="col-6">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Teacher.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Teacher.Name)
            </dd>
        </dl>
        <div class="col-6">
            @(await Html.RenderComponentAsync<SchedulesList>(RenderMode.ServerPrerendered,new { TeacherId = Model.Teacher.Id }))
        </div>
    </div>

    <form method="post">
        <input type="hidden" asp-for="Teacher.Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-page="./Index">Вернуться к списку Преподавателей</a>
    </form>
</div>
