@page
@model StudentsEducation.Web.Areas.Admin.Pages.Schedules.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Schedule[0].StartsIn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schedule[0].EndsIn)
            </th>
            <th>
                @Html.DisplayNameFor(model=>model.Schedule[0].Subject.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schedule[0].Group.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Schedule[0].Teacher.Name)
            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Schedule) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StartsIn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndsIn)
            </td>
            <td>
                @Html.DisplayFor(modelItem=>item.Subject.Name)
            </td>
            <td>
                @Html.DisplayFor(modelITem=>item.Group.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
